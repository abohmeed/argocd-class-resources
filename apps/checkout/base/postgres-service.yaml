apiVersion: v1
kind: Service
metadata:
  name: checkout-postgres
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: checkout-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
